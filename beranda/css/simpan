.responsive-beranda .footer-mobile {
  border-top: 1px solid var(--other-outline-border, rgba(231, 227, 252, 0.23));
  background: var(--other-page-header, #181A1C);
  display: flex;
  flex-direction: column;
  align-self: flex-start;
  padding: 18.4px 28px 22px 20px;
  width: 100%;
  box-sizing: border-box;
}